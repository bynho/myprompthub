import React from 'react';

// This component is no longer needed but kept as an empty component to avoid breaking imports
const TranslationWarning: React.FC<{ show: boolean }> = () => {
  return null;
};

export default TranslationWarning;